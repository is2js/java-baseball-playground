//2. Application이외에는 추가 패키지로 영역을 나누어서 class를 생성한다.
// - 비지니스 로직(계산)등은 domain or model에 속한다.
package baseball.domain;

//
public class Ball {
    //3. 생성자를 [alt+insert]의 단축키로 만들어본다.
    public Ball() {

    }
    //4. 메소드는 소문자로 시작한다.
    //- 자바에서는 기능을 함수가 아닌 메서드라고 부른다.
    // -> domain패키지에다가 Calculator로 새로 시작해보자.


}
