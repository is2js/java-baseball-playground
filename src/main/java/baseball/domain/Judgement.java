package baseball.domain;

import java.util.List;

public class Judgement {
    //3. correctCount() -> 몇개의 숫자가 같은지 int로 [결과값]을 알려준다.
    //  -> 몇개가 맞았는지 알려줘야 하므로 -> 파라미터로 컴퓨터의수, 플레이어의수를 받아서 처리할 것이다!!
    public int correctCount(List<Integer> computer, List<Integer> player) {
        return 0;
    }
    //4.
    // hasPlace -> boolean타입으로 [결과값]을 알려준다.
    // -> 특정자리에 특정 숫자? 특정자리(int-index)에, 특정숫자(int)까지 다 받아야한다.
    public boolean hasPlace(int place, int number) {
        return false;
    }
    //5. 나머지들은 Test를 통해 [결과값]을 확인해봐야한다.
//          - [ ] 같은 수가 같은 자리에 있으면 스트라이크이다.
//            - [ ] 같은 수가 다른 자리에 있으면 볼이다.
//            - [ ] 같은 수가 전혀 없으면 낫싱이다.
    // 6. 첫 커밋은 뼈대가 다 완성되면 커밋한다.

}
